# Sortable Array Exercise

This repository contains two sorting features implemented in Java:

1. **Integer Sorting** - Uses Quick Sort to sort an array of integers.
   - **Class**: `IntegerSorter`
   - **Usage**: Run `IntegerSorter.java` to sort an integer array.
  
2. **String Sorting** - Uses Bubble Sort to sort an array of strings alphabetically.
   - **Class**: `StringSorter`
   - **Usage**: Run `StringSorter.java` to sort a string array.

Each feature is tested with a sample input in the main method.

