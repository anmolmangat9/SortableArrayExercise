// src/SortableArray.java
import java.util.Arrays;

public class SortableArray {

    // Method to sort an array of integers
    public int[] sortIntegers(int[] numbers) {
        Arrays.sort(numbers);
        return numbers;
    }
}
